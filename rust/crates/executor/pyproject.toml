[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "pms-executor-rs"
requires-python = ">=3.11"
dynamic = ["version"]

[tool.maturin]
features = ["extension-module"]
